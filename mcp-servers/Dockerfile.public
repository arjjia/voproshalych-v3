FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml .
RUN uv pip install --system -e .

COPY src/ src/

ENV PYTHONPATH=/app/src
CMD ["sh", "-c", "MCP_SERVER_TYPE=${MCP_SERVER_TYPE} python -m src.public.server"]
